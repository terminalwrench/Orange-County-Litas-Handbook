export interface RideRecord {
  id: string;
  eventId?: string;
  title: string;
  date: string;
  time?: string;
  meetup: string;
  destination: string;
  mileage: string;
  duration: string;
  difficulty: string;
  notes: string;
}
